# resources/ —— 资源

| 目录 | 说明 |
|------|------|
| `assets/` | 源文件：`logo.svg` 是应用图标源图，改它后跑 `pnpm icons` 生成全套图标 |
| `public/` | 静态资源：favicon、多尺寸 logo（构建时复制到输出） |

改品牌：替换 `assets/logo.svg` → `pnpm icons`。
